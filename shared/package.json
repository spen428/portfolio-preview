{
  "name": "shared",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:prod": "pnpm run build",
    "prepare": "pnpm run build:prod",
    "dev": "tsc --watch"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
